#include "main.h"
#include <string.h>

/**
 * _strcmp - a function that compares two strings
 * @s1: param1
 * @s2: param2
 * Return: a string
 */

int _strcmp(char *s1, char *s2)
{
	strcmp (s1, s2);
	return (s1);
}
